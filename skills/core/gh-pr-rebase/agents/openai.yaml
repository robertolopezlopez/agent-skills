interface:
  display_name: "GitHub PR Rebase"
  short_description: "Check PR conflicts and rebase safely"
  default_prompt: "Use $gh-pr-rebase to update this conflicted or out-of-date pull request while preserving its source changes."
