interface:
  display_name: "CLI CI Monitor"
  short_description: "Monitor and diagnose CLI CircleCI workflows"
  default_prompt: "Use $cli-ci-monitor to monitor this CLI CircleCI workflow, retry transient failures, and diagnose final non-transient failures."
